name: Packaging target inventory

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate package target inventory
        run: python3 scripts/check-packaging-targets.py
      - name: Verify release artifact and distro gates
        run: python3 tests/release-workflow.test.py
