import{_ as o,o as t,c as s,a2 as a}from"./chunks/framework.BfI5QxtK.js";const k=JSON.parse('{"title":"Session locking","description":"","frontmatter":{},"headers":[],"relativePath":"session-lock.md","filePath":"session-lock.md"}'),n={name:"session-lock.md"};function r(c,e,i,l,d,h){return t(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="session-locking" tabindex="-1">Session locking <a class="header-anchor" href="#session-locking" aria-label="Permalink to &quot;Session locking&quot;">​</a></h1><p>Gnoblin uses the standard <code>ext-session-lock-v1</code> Wayland protocol in the Gnoblin session. The compositor owns the security boundary: it covers every output, stops normal input, replaces desktop capture with its lock scene, and stays locked if a locker exits. The locker owns its appearance and authentication.</p><p>Gnoblin does not provide a default lock screen. Bingux has its own independent lock client. Third-party lockers such as hyprlock, swaylock, gtklock and waylock can connect directly to the same standard protocol; the first locker to request a lock owns it, and concurrent lockers receive <code>finished</code>.</p><p>In Bingux, use its Lock action or run <code>bingux-lock</code>. With another locker installed, run its command (for example, <code>hyprlock</code>) in your Gnoblin session. Your shell or idle daemon decides when to run that command; Gnoblin does not set a timeout or launch a locker for you.</p><p>The regular GNOME session is separate. It does not advertise this protocol and continues to use GNOME ScreenShield.</p><h2 id="portal-and-remote-access-while-locked" tabindex="-1">Portal and remote access while locked <a class="header-anchor" href="#portal-and-remote-access-while-locked" aria-label="Permalink to &quot;Portal and remote access while locked&quot;">​</a></h2><p>Existing portal permission remains in force. A monitor stream already authorised through the desktop portal receives the compositor lock scene while the session is locked, rather than normal desktop content. Remote keyboard and pointer input is available after <code>locked</code> and lock-scene presentation, and is routed only to the active lock surface. It is refused during the transition and failsafe states. This requires no lock-specific portal setting, opt-in, or change to an existing authorised grant.</p><h2 id="locker-requirements" tabindex="-1">Locker requirements <a class="header-anchor" href="#locker-requirements" aria-label="Permalink to &quot;Locker requirements&quot;">​</a></h2><p>A locker must wait for <code>locked</code> before treating the session as secure. It must create one lock surface for each output, acknowledge each configure, and commit a buffer of the configured output-local size. After successful authentication, only the owner calls <code>unlock_and_destroy</code>. A locker crash leaves the compositor covered and locked; another client may acquire the lock afterwards.</p>',9)])])}const p=o(n,[["render",r]]);export{k as __pageData,p as default};
