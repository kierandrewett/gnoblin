import{_ as t,o as s,c as d,a2 as i}from"./chunks/framework.D7WGirly.js";const u=JSON.parse('{"title":"Shell D-Bus API","description":"","frontmatter":{},"headers":[],"relativePath":"shell-dbus-api.md","filePath":"shell-dbus-api.md"}'),o={name:"shell-dbus-api.md"};function a(n,e,r,c,l,h){return s(),d("div",null,[...e[0]||(e[0]=[i(`<h1 id="shell-d-bus-api" tabindex="-1">Shell D-Bus API <a class="header-anchor" href="#shell-d-bus-api" aria-label="Permalink to &quot;Shell D-Bus API&quot;">​</a></h1><p><code>org.gnoblin.Shell</code> is Gnoblin&#39;s session-bus interface for shell status, input-source selection, feature switches and permission state. The service owns <code>/org/gnoblin/Shell</code> while the shell component is running.</p><p>Use <a href="/gnoblinctl"><code>gnoblinctl</code></a> for command-line and script use. Shells that need live window state or temporary input grabs should use the <a href="/compositor-bridge">compositor bridge</a>.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdbus</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> introspect</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --session</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> org.gnoblin.Shell</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --object-path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /org/gnoblin/Shell</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdbus</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> call</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --session</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> org.gnoblin.Shell</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --object-path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /org/gnoblin/Shell</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --method</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> org.gnoblin.Shell.Ping</span></span></code></pre></div><p><code>Ping</code> returns <code>(&#39;pong&#39;,)</code>. <code>GetVersion</code> returns the running shell version string, such as <code>51.0-gnoblin</code>. Use <code>gnoblinctl version</code> for the installed Gnoblin package version.</p><h2 id="methods" tabindex="-1">Methods <a class="header-anchor" href="#methods" aria-label="Permalink to &quot;Methods&quot;">​</a></h2><p>Signatures use D-Bus type notation. Methods with no output return an empty tuple.</p><table tabindex="0"><thead><tr><th>Method</th><th>Input → output</th><th>Purpose</th></tr></thead><tbody><tr><td><code>Ping</code></td><td><code>() → s</code></td><td>Liveness check; returns <code>pong</code>.</td></tr><tr><td><code>GetVersion</code></td><td><code>() → s</code></td><td>Running shell version string, such as <code>51.0-gnoblin</code>.</td></tr><tr><td><code>Reload</code></td><td><code>() → ()</code></td><td>Soft-reload theme and scripts while keeping application windows.</td></tr><tr><td><code>ReloadConfig</code></td><td><code>() → ()</code></td><td>Reload the active Lua configuration.</td></tr><tr><td><code>ListInputSources</code></td><td><code>() → a(ssss)</code></td><td>List input sources as <code>(type, id, shortName, displayName)</code>.</td></tr><tr><td><code>GetCurrentInputSource</code></td><td><code>() → (ssss)</code></td><td>Return the current input source in the same field order.</td></tr><tr><td><code>SetInputSource</code></td><td><code>(ss) → ()</code></td><td>Activate the source identified by <code>(type, id)</code> from the list.</td></tr><tr><td><code>GetPrivacyState</code></td><td><code>() → (bbb)</code></td><td>Read screen sharing, microphone use and location use.</td></tr><tr><td><code>ListScripts</code></td><td><code>() → as</code></td><td>List successfully loaded script names.</td></tr><tr><td><code>GetPermissions</code></td><td><code>() → s</code></td><td>Return a JSON string containing the active policy, capabilities, levels and config path.</td></tr><tr><td><code>CheckPermission</code></td><td><code>(ss) → (ssasub)</code></td><td>Evaluate <code>(capability, identity)</code>; return <code>(level, rule, monitors, devices, clipboard)</code>.</td></tr><tr><td><code>ListPortalGrants</code></td><td><code>() → a(sssubb)</code></td><td>List grants as <code>(id, portal, requester, devices, clipboard, screenStreams)</code>.</td></tr><tr><td><code>RevokePortalGrant</code></td><td><code>(ss) → ()</code></td><td>Revoke a grant by <code>(portal, id)</code>. Portal is <code>screen-cast</code> or <code>remote-desktop</code>.</td></tr><tr><td><code>ListFeatures</code></td><td><code>() → a(ssb)</code></td><td>List feature <code>(id, description, enabled)</code> records.</td></tr><tr><td><code>GetFeature</code></td><td><code>(s) → b</code></td><td>Read a feature state; an unknown ID returns <code>false</code>.</td></tr><tr><td><code>SetFeature</code></td><td><code>(sb) → ()</code></td><td>Persist and apply a supported feature switch.</td></tr></tbody></table><p>Errors:</p><ul><li><code>Reload</code> and <code>ReloadConfig</code> report failures as D-Bus errors.</li><li><code>CheckPermission</code> reports invalid requests as <code>org.gnoblin.Shell.Error.PermissionPolicy</code>.</li><li><code>SetFeature</code> reports unknown IDs and attempts to enable removed native UI features as errors. Feature IDs are runtime data; discover them with <code>ListFeatures</code> or <code>gnoblinctl feature list</code>.</li></ul><p>For permission identities and policy fields, see the <a href="/guides/permissions">permission guide</a>. Do not treat a portal grant as a permission-policy rule: grants represent user-approved portal sessions.</p><h2 id="properties" tabindex="-1">Properties <a class="header-anchor" href="#properties" aria-label="Permalink to &quot;Properties&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Property</th><th>Type</th><th>Meaning</th></tr></thead><tbody><tr><td><code>IsWayland</code></td><td><code>b</code></td><td>Whether this shell is running as a Wayland compositor.</td></tr><tr><td><code>SessionMode</code></td><td><code>s</code></td><td>Current GNOME Shell session mode; Gnoblin sessions report <code>gnoblin</code>.</td></tr></tbody></table><p>Read a property with <code>gdbus</code>:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gdbus</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> call</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --session</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> org.gnoblin.Shell</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --object-path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /org/gnoblin/Shell</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --method</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> org.freedesktop.DBus.Properties.Get</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    org.gnoblin.Shell</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> IsWayland</span></span></code></pre></div><h2 id="signals" tabindex="-1">Signals <a class="header-anchor" href="#signals" aria-label="Permalink to &quot;Signals&quot;">​</a></h2><p>Listen with <code>gdbus monitor --session --dest org.gnoblin.Shell</code>. Signals have no replay: connect first, then read the corresponding method for current state.</p><table tabindex="0"><thead><tr><th>Signal</th><th>Fields</th><th>Meaning</th></tr></thead><tbody><tr><td><code>SuperReleased</code></td><td><code>(u, t)</code></td><td>Super was released without another input. Includes protocol version <code>1</code> and a monotonic timestamp in microseconds.</td></tr><tr><td><code>OsdRequested</code></td><td><code>(u, i, s, s, d, d, as)</code></td><td>OSD protocol version <code>2</code>, monitor index, icon, label, level, maximum level and physical output names for that logical monitor.</td></tr><tr><td><code>InputSourceChanged</code></td><td><code>(s, s, s, s)</code></td><td>Current input source changed: <code>(type, id, shortName, displayName)</code>.</td></tr><tr><td><code>InputSourcesChanged</code></td><td><code>b</code></td><td>The value is <code>true</code>; refresh the source list with <code>ListInputSources</code>.</td></tr><tr><td><code>PrivacyStateChanged</code></td><td><code>(b, b, b)</code></td><td>Screen sharing, microphone use and location use changed.</td></tr><tr><td><code>FeatureChanged</code></td><td><code>(s, b)</code></td><td>A feature ID and its new enabled state.</td></tr></tbody></table><p><code>InputSourcesChanged</code> is followed by <code>InputSourceChanged</code> for the current source. The initial privacy and feature states are not emitted as a snapshot; call <code>GetPrivacyState</code> and <code>ListFeatures</code> when connecting. The OSD signal replaces the removed in-process OSD UI; an external shell decides how to display it.</p><p>For the versioned JSON socket used by window lists, actions, previews and temporary grabs, see the <a href="/compositor-bridge">compositor bridge</a>. For launch feedback tokens, see the <a href="/launch-feedback">launch feedback interface</a>.</p>`,20)])])}const g=t(o,[["render",a]]);export{u as __pageData,g as default};
