import{_ as o,o as t,c as n,a2 as s}from"./chunks/framework.BfI5QxtK.js";const u=JSON.parse('{"title":"Session locking design","description":"","frontmatter":{},"headers":[],"relativePath":"session-lock-design.md","filePath":"session-lock-design.md"}'),a={name:"session-lock-design.md"};function i(r,e,c,l,d,h){return t(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="session-locking-design" tabindex="-1">Session locking design <a class="header-anchor" href="#session-locking-design" aria-label="Permalink to &quot;Session locking design&quot;">​</a></h1><p>Gnoblin is moving session locking from GNOME Shell&#39;s <code>ScreenShield</code> to a compositor-enforced Wayland lock. This page records the security contract and the rollout gates. <strong>The running Gnoblin session still uses GNOME&#39;s lock screen until the compositor passes the protocol checks below.</strong> A vendored protocol XML file, a build that succeeds, or a lock window that looks correct does not establish a secure lock.</p><h2 id="why-a-compositor-lock" tabindex="-1">Why a compositor lock <a class="header-anchor" href="#why-a-compositor-lock" aria-label="Permalink to &quot;Why a compositor lock&quot;">​</a></h2><p>An ordinary fullscreen or layer-shell window cannot guarantee that the desktop underneath is hidden, that every output is covered, or that keyboard, pointer, touch, shortcuts and capture stop reaching ordinary clients. The compositor must make these decisions before it acknowledges a lock. The standard <a href="https://wayland.app/protocols/ext-session-lock-v1" target="_blank" rel="noreferrer"><code>ext-session-lock-v1</code> protocol</a> defines this boundary. Its <code>locked</code> event is sent only after a locked frame has been presented on every output. If the client dies, the compositor stays locked. <a href="https://github.com/hyprwm/Hyprland/blob/main/src/managers/SessionLockManager.cpp" target="_blank" rel="noreferrer">Hyprland uses this split</a>: the compositor enforces the lock, while <a href="https://github.com/hyprwm/hyprlock" target="_blank" rel="noreferrer">hyprlock</a> supplies the graphics and authentication.</p><p>Mutter does not currently provide this protocol in Gnoblin. The XML under <code>src/protocols/session-lock/</code> is a specification, not an advertised global. The compositor implementation must be validated before any external lock client replaces <code>ScreenShield</code>.</p><h2 id="responsibilities" tabindex="-1">Responsibilities <a class="header-anchor" href="#responsibilities" aria-label="Permalink to &quot;Responsibilities&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Component</th><th>Owns</th></tr></thead><tbody><tr><td>Gnoblin&#39;s Mutter fork</td><td>Lock state, opaque fallback on every output, input and capture isolation, lock surface placement, and the <code>ext-session-lock-v1</code> server</td></tr><tr><td>Bingux lock client, current</td><td>Lock-screen appearance, accessible prompts, authentication flow, and <code>unlock_and_destroy</code> after successful authentication</td></tr><tr><td>Bingux policy, future</td><td>Optional manual, idle, logind, sleep, timeout, inhibitor, and compatibility D-Bus integration</td></tr><tr><td>GDM and logind</td><td>Login/greeter and system session management; both remain installed</td></tr></tbody></table><p>The Bingux desktop shell process is separate from its lock client. Reloading the bar, dock or settings cannot dismiss a lock. The compositor owns the blank fallback, so a crashed Bingux lock client cannot expose the desktop. Another shell can supply a different conforming lock client.</p><h2 id="compositor-state-machine" tabindex="-1">Compositor state machine <a class="header-anchor" href="#compositor-state-machine" aria-label="Permalink to &quot;Compositor state machine&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>unlocked → covering → locked → unlocked</span></span>
<span class="line"><span>                  ↘ failsafe ↗</span></span></code></pre></div><p>On a valid <code>lock()</code> request, the compositor enters <strong>covering</strong> immediately: normal content and input are suppressed, and an opaque compositor-owned cover is scheduled on every output. It may wait briefly for lock surfaces. It enters <strong>locked</strong> and sends the protocol event only after a locked frame or opaque fallback has actually been presented on every output. A missing surface never extends this wait indefinitely.</p><p>While locked, only the active lock client&#39;s correctly configured surfaces and explicit compositor-owned UI may be visible. The compositor rejects a second lock owner. New or resized outputs get an opaque cover before any normal frame; the client then receives a new <code>configure</code>. Destroying a lock surface or losing the client leaves the session locked and covered. Recovery starts another trusted client while the cover remains, or requires ending the session from a separate VT. The dead client&#39;s disappearance never counts as authentication.</p><p>Only the owning live lock object can send <code>unlock_and_destroy</code>, and only after <code>locked</code>. The client waits for a <code>wl_display.sync</code> round trip before exiting. The compositor removes covers and restores focus/input together. A lock client must wait for each surface&#39;s first <code>configure</code>, acknowledge the serial, and commit a buffer at the configured output-local size. Lock UI popups and other ordinary surfaces do not get special treatment.</p><h2 id="policy-and-system-integration" tabindex="-1">Policy and system integration <a class="header-anchor" href="#policy-and-system-integration" aria-label="Permalink to &quot;Policy and system integration&quot;">​</a></h2><p>The current Bingux lock client does not yet own <code>loginctl</code>, idle timeouts, suspend handling, <code>org.gnome.ScreenSaver</code>, <code>org.freedesktop.ScreenSaver</code>, or logind inhibitors. A user may run an idle-only tool such as hypridle alongside a conforming lock client; that is a policy choice outside Gnoblin and does not establish D-Bus or suspend compatibility.</p><p>Future Bingux policy may serialize manual, idle, logind, desktop-control, and pre-suspend requests, expose compatibility APIs, and honour idle inhibitors. If it implements pre-suspend locking, it must hold a logind <strong>delay inhibitor</strong> until compositor presentation is confirmed. A timeout, process start, or visual animation cannot substitute for the protocol&#39;s <code>locked</code> event.</p><p>GNOME Shell&#39;s <code>ScreenShield</code>, its D-Bus owner and its idle/sleep listeners are removed <strong>only in the Gnoblin session</strong> after the replacement is verified. The regular GNOME login continues using GNOME&#39;s lock screen. GDM, <code>gnome-session</code> and settings-daemon are retained. Gnoblin&#39;s own bridge and recovery controls must query the new lock state before exposing operations that <code>Main.sessionMode.isLocked</code> currently protects.</p><h2 id="shell-cutover-contract" tabindex="-1">Shell cutover contract <a class="header-anchor" href="#shell-cutover-contract" aria-label="Permalink to &quot;Shell cutover contract&quot;">​</a></h2><p>GNOME Shell keeps its normal path whenever the compositor has not proved a secure lock protocol. In the Gnoblin session, Shell reads Mutter&#39;s native <code>get_gnoblin_session_lock_capability()</code> accessor at startup and skips constructing <code>ScreenShield</code> only when it reports:</p><ol><li><code>CapabilityVersion &gt;= 1</code>;</li><li>a server with compositor-enforced <code>unlocked</code>, <code>covering</code>, <code>locked</code>, and <code>failsafe</code> states is installed; and</li><li>all presentation, input isolation, client-death, and capture gates are secure.</li></ol><p>The native capability defaults to zero while the protocol global is hidden. A missing accessor or false result retains GNOME ScreenShield. The regular GNOME session always retains its own ScreenShield.</p><p>The native manager will advertise the standard <code>ext-session-lock-v1</code> global to any client in the same session. Bingux and compatible clients such as hyprlock may acquire the same protocol role directly. The global remains hidden in the current build until the compositor has passed the secure coverage, input isolation, client-death, and presentation checks in this document. No runtime compatibility claim is made before those tests pass.</p><p>The native seam supplies <code>get_gnoblin_session_lock_active()</code>, which is true from <code>covering</code> through <code>failsafe</code>. Shell&#39;s bridge stops work as soon as Mutter installs its input embargo rather than waiting for presentation confirmation. Bingux may add policy, compatibility APIs, and logind integration later. Gnoblin neither launches a locker nor owns session policy.</p><p>Gnoblin&#39;s bridge and developer console use the same adapter for their locked state. It combines stock <code>sessionMode.isLocked</code> with the compositor&#39;s active state, cancels bridge interaction when the compositor becomes active, and refuses screenshots, previews and window operations while locked. A compositor which is absent, too old, <code>covering</code>, or <code>unavailable</code> leaves GNOME&#39;s normal ScreenShield untouched.</p><p>The Shell screenshot service applies that same predicate before creating a <code>Shell.Screenshot</code>, opening screenshot or recording UI, interactive capture, and area selection. It returns permission denied over <code>org.gnome.Shell.Screenshot</code> from <code>covering</code> onward. This complements the compositor&#39;s capture isolation and prevents Shell-owned screenshot paths from leaking a frame during the handover.</p><p>The Shell suppresses GNOME&#39;s “Screen Lock disabled” warning only after that authority check. Until Bingux explicitly integrates a Lock Screen action, the Shell hides it in cutover rather than routing a lock request to an unspecified native policy owner. Switch User is likewise unavailable in cutover because its old path locks <code>ScreenShield</code>.</p><h2 id="release-gates" tabindex="-1">Release gates <a class="header-anchor" href="#release-gates" aria-label="Permalink to &quot;Release gates&quot;">​</a></h2><h3 id="compositor-protocol" tabindex="-1">Compositor protocol <a class="header-anchor" href="#compositor-protocol" aria-label="Permalink to &quot;Compositor protocol&quot;">​</a></h3><p>Gnoblin may expose <code>ext-session-lock-v1</code> only after a fresh installed session proves all of these paths:</p><ol><li>A conforming client acquires one owner and repeated requests cannot create competing lock owners.</li><li>No app receives keyboard, pointer, touch, shortcut, clipboard or remote input while locked; capture and portal paths cannot reveal normal content.</li><li>Lock UI crash, forced kill, hang before its first buffer, output hotplug, scale/rotation changes and GPU reset stay opaque and locked.</li><li>Real multi-monitor hardware and a fresh session confirm the <code>locked</code> event follows presentation, and ordinary input/focus returns only on unlock.</li></ol><h3 id="optional-bingux-policy" tabindex="-1">Optional Bingux policy <a class="header-anchor" href="#optional-bingux-policy" aria-label="Permalink to &quot;Optional Bingux policy&quot;">​</a></h3><p>Manual shortcuts, <code>loginctl</code>, desktop controls, D-Bus compatibility, idle timeouts and inhibition, and lock-before-suspend/hibernate/lid-close are separate Bingux work. Each requires its own end-to-end evidence, including a presentation-confirmed delay inhibitor before suspend. Authentication success, failure, cancellation, and multi-prompt PAM or biometric conversations belong to the selected lock client and must be tested before that client is promoted.</p><p>The previous GNOME lock remains the default while any gate is open. A nested headless test can check protocol ordering but cannot prove display scanout, hotplug, VT or resume behaviour.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><a href="https://wayland.app/protocols/ext-session-lock-v1" target="_blank" rel="noreferrer">Wayland session-lock protocol and lifecycle</a></li><li><a href="https://github.com/hyprwm/Hyprland/blob/main/src/managers/SessionLockManager.cpp" target="_blank" rel="noreferrer">Hyprland session lock manager</a></li><li><a href="https://github.com/hyprwm/hyprlock" target="_blank" rel="noreferrer">hyprlock client</a> and <a href="https://github.com/hyprwm/hypridle" target="_blank" rel="noreferrer">hypridle policy</a></li><li><a href="https://www.freedesktop.org/software/systemd/man/latest/org.freedesktop.login1.Session.html" target="_blank" rel="noreferrer">systemd logind session API</a></li><li><a href="https://github.com/systemd/systemd/blob/main/docs/INHIBITOR_LOCKS.md" target="_blank" rel="noreferrer">systemd inhibitor locks</a></li><li><a href="https://gitlab.gnome.org/GNOME/gnome-shell/-/blob/main/js/ui/screenShield.js" target="_blank" rel="noreferrer">GNOME Shell <code>ScreenShield</code></a></li></ul>`,35)])])}const m=o(a,[["render",i]]);export{u as __pageData,m as default};
