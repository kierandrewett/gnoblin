import{_ as t,o,c as d,a2 as n}from"./chunks/framework.BfI5QxtK.js";const h=JSON.parse('{"title":"gnoblin.configure.shell","description":"","frontmatter":{},"headers":[],"relativePath":"config/configure/shell.md","filePath":"config/configure/shell.md"}'),i={name:"config/configure/shell.md"};function a(c,e,s,r,l,u){return o(),d("div",null,[...e[0]||(e[0]=[n('<h1 id="gnoblin-configure-shell" tabindex="-1">gnoblin.configure.shell <a class="header-anchor" href="#gnoblin-configure-shell" aria-label="Permalink to &quot;gnoblin.configure.shell&quot;">​</a></h1><p>Configure this part of <code>gnoblin.configure</code> with the <code>shell</code> key.</p><p>Inside <code>gnoblin.configure {shell = {...}}</code>.</p><table tabindex="0"><thead><tr><th>Key</th><th>Values</th><th>Default</th></tr></thead><tbody><tr><td><code>minimize_animation</code></td><td>Built-in animation name or <code>{minimize = name, restore = name}</code></td><td><code>&quot;zoom&quot;</code></td></tr><tr><td><code>minimize_duration</code></td><td>0–5000 ms</td><td><code>200</code></td></tr><tr><td><code>minimize_target</code></td><td><code>{x, y}</code> in desktop logical pixels</td><td>Dock target, then bottom centre</td></tr><tr><td><code>layer_animation</code></td><td>Built-in animation name or event map for <code>layer-open</code> and <code>layer-close</code></td><td><code>&quot;slide&quot;</code></td></tr><tr><td><code>layer_duration</code></td><td>0–5000 ms</td><td><code>220</code></td></tr><tr><td><code>layer_easing</code></td><td><code>&quot;linear&quot;</code>, <code>&quot;ease-out-quad&quot;</code>, <code>&quot;ease-out-cubic&quot;</code>, <code>&quot;ease-in-out-cubic&quot;</code></td><td><code>&quot;ease-out-cubic&quot;</code></td></tr><tr><td><code>window_menu</code></td><td>Command argument list</td><td>Empty</td></tr><tr><td><code>window_switcher</code></td><td>Boolean</td><td><code>false</code></td></tr><tr><td><code>notifications</code></td><td>Boolean</td><td>Initially disabled; persists in GSettings</td></tr><tr><td><code>input_source_switcher</code></td><td>Boolean</td><td>Initially disabled; persists in GSettings</td></tr></tbody></table><p>Guides: <a href="/gnoblin/guides/animations">animations</a>, <a href="/gnoblin/guides/session_settings">native features</a>, <a href="/gnoblin/guides/window_menu">window menu</a>.</p><p>Animation names include <code>slide</code>, <code>fade</code>, <code>zoom</code>, <code>none</code>, and the <code>gnome-*</code> and <code>gnoblin-*</code> presets. Use event maps when entry and exit need different animations. Declare custom animations with <a href="/gnoblin/config/animation"><code>gnoblin.animation</code></a>; the <a href="/gnoblin/guides/animations">animation guide</a> lists events and presets. The <code>layer_animation</code> map accepts <code>in</code>/<code>out</code> as aliases for <code>layer-open</code> and <code>layer-close</code>.</p><p><code>minimize_target</code> is a <code>{x, y}</code> position in logical desktop pixels. Coordinates must be integers whose absolute value is at most 1,000,000. If no dock target is available, Gnoblin uses the bottom-centre of the screen. <code>window_menu</code> is a command argv list (up to 32 arguments); an empty list disables it. Arguments are passed directly, without shell expansion. <code>window_switcher</code> controls GNOME&#39;s app, window and group switchers. Leave it disabled when another shell component provides those interfaces.</p>',7)])])}const g=t(i,[["render",a]]);export{h as __pageData,g as default};
