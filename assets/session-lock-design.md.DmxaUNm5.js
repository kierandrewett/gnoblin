import{_ as o,o as t,c as n,a2 as s}from"./chunks/framework.BfI5QxtK.js";const u=JSON.parse('{"title":"Session locking design","description":"","frontmatter":{},"headers":[],"relativePath":"session-lock-design.md","filePath":"session-lock-design.md"}'),a={name:"session-lock-design.md"};function r(i,e,c,l,d,h){return t(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="session-locking-design" tabindex="-1">Session locking design <a class="header-anchor" href="#session-locking-design" aria-label="Permalink to &quot;Session locking design&quot;">​</a></h1><p>Gnoblin is moving session locking from GNOME Shell&#39;s <code>ScreenShield</code> to a compositor-enforced Wayland lock. This page records the security contract and the rollout gates. <strong>The running Gnoblin session still uses GNOME&#39;s lock screen until the compositor and Bingux client pass the checks below.</strong> A vendored protocol XML file, a build that succeeds, or a lock window that looks correct does not establish a secure lock.</p><h2 id="why-a-compositor-lock" tabindex="-1">Why a compositor lock <a class="header-anchor" href="#why-a-compositor-lock" aria-label="Permalink to &quot;Why a compositor lock&quot;">​</a></h2><p>An ordinary fullscreen or layer-shell window cannot guarantee that the desktop underneath is hidden, that every output is covered, or that keyboard, pointer, touch, shortcuts and capture stop reaching ordinary clients. The compositor must make these decisions before it acknowledges a lock. The standard <a href="https://wayland.app/protocols/ext-session-lock-v1" target="_blank" rel="noreferrer"><code>ext-session-lock-v1</code> protocol</a> defines this boundary. Its <code>locked</code> event is sent only after a locked frame has been presented on every output. If the client dies, the compositor stays locked. <a href="https://github.com/hyprwm/Hyprland/blob/main/src/managers/SessionLockManager.cpp" target="_blank" rel="noreferrer">Hyprland uses this split</a>: the compositor enforces the lock, while <a href="https://github.com/hyprwm/hyprlock" target="_blank" rel="noreferrer">hyprlock</a> supplies the graphics and authentication.</p><p>Mutter does not currently provide this protocol in Gnoblin. The XML under <code>src/protocols/session-lock/</code> is a specification, not an advertised global. The compositor implementation must be validated before any external lock client replaces <code>ScreenShield</code>.</p><h2 id="responsibilities" tabindex="-1">Responsibilities <a class="header-anchor" href="#responsibilities" aria-label="Permalink to &quot;Responsibilities&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Component</th><th>Owns</th></tr></thead><tbody><tr><td>Gnoblin&#39;s Mutter fork</td><td>Lock state, opaque fallback on every output, input and capture isolation, lock surface placement, and the <code>ext-session-lock-v1</code> server</td></tr><tr><td>Gnoblin lock coordinator</td><td>Manual, idle, logind and sleep requests; timeout and inhibitor policy; launching one configured lock client; compatibility D-Bus APIs; reporting actual locked state</td></tr><tr><td>Bingux lock client</td><td>One lock surface per output, visual design, accessible prompts, authentication, and <code>unlock_and_destroy</code> after successful authentication</td></tr><tr><td>GDM and logind</td><td>Login/greeter and system session management; both remain installed</td></tr></tbody></table><p>The Bingux desktop shell process is separate from its lock client. Reloading the bar, dock or settings cannot dismiss a lock. The compositor owns the blank fallback, so a crashed Bingux lock client cannot expose the desktop. Another shell can supply a different conforming lock client.</p><h2 id="compositor-state-machine" tabindex="-1">Compositor state machine <a class="header-anchor" href="#compositor-state-machine" aria-label="Permalink to &quot;Compositor state machine&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>unlocked → covering → locked → unlocked</span></span>
<span class="line"><span>                  ↘ failsafe ↗</span></span></code></pre></div><p>On a valid <code>lock()</code> request, the compositor enters <strong>covering</strong> immediately: normal content and input are suppressed, and an opaque compositor-owned cover is scheduled on every output. It may wait briefly for lock surfaces. It enters <strong>locked</strong> and sends the protocol event only after a locked frame or opaque fallback has actually been presented on every output. A missing surface never extends this wait indefinitely.</p><p>While locked, only the active lock client&#39;s correctly configured surfaces and explicit compositor-owned UI may be visible. The compositor rejects a second lock owner. New or resized outputs get an opaque cover before any normal frame; the client then receives a new <code>configure</code>. Destroying a lock surface or losing the client leaves the session locked and covered. Recovery starts another trusted client while the cover remains, or requires ending the session from a separate VT. The dead client&#39;s disappearance never counts as authentication.</p><p>Only the owning live lock object can send <code>unlock_and_destroy</code>, and only after <code>locked</code>. The client waits for a <code>wl_display.sync</code> round trip before exiting. The compositor removes covers and restores focus/input together. A lock client must wait for each surface&#39;s first <code>configure</code>, acknowledge the serial, and commit a buffer at the configured output-local size. Lock UI popups and other ordinary surfaces do not get special treatment.</p><h2 id="policy-and-system-integration" tabindex="-1">Policy and system integration <a class="header-anchor" href="#policy-and-system-integration" aria-label="Permalink to &quot;Policy and system integration&quot;">​</a></h2><p>The coordinator serializes <code>loginctl lock-session</code>, the lock shortcut, desktop controls, idle timeout and pre-suspend lock into one request path. It preserves the useful <code>org.gnome.ScreenSaver</code> and <code>org.freedesktop.ScreenSaver</code> methods for applications and existing idle inhibitors. An idle inhibitor can defer an idle lock; it cannot cancel an explicit manual or logind lock. <code>SetLockedHint</code> tells logind the result, but does not itself lock anything.</p><p>For suspend, the coordinator holds a logind <strong>delay inhibitor</strong> before sleep is requested, requests a lock on <code>PrepareForSleep(true)</code>, and releases that delay only after secure presentation is confirmed. It renews the inhibitor after wake. A timeout is a failure to report, not permission to expose the desktop. This is why a fixed sleep, a process-start event, or a visual animation cannot stand in for the protocol&#39;s <code>locked</code> event.</p><p>GNOME Shell&#39;s <code>ScreenShield</code>, its D-Bus owner and its idle/sleep listeners are removed <strong>only in the Gnoblin session</strong> after the replacement is verified. The regular GNOME login continues using GNOME&#39;s lock screen. GDM, <code>gnome-session</code> and settings-daemon are retained. Gnoblin&#39;s own bridge and recovery controls must query the new lock state before exposing operations that <code>Main.sessionMode.isLocked</code> currently protects.</p><h2 id="shell-cutover-contract" tabindex="-1">Shell cutover contract <a class="header-anchor" href="#shell-cutover-contract" aria-label="Permalink to &quot;Shell cutover contract&quot;">​</a></h2><p>The first implementation keeps GNOME Shell&#39;s path by default. A developer can set <code>GNOBLIN_SESSION_LOCK_CUTOVER=1</code> only for a fresh Gnoblin Shell process. At startup, Shell reads Mutter&#39;s native <code>get_gnoblin_session_lock_capability()</code> accessor. It does not synchronously query D-Bus: Mutter and the Shell JavaScript share one process, so a startup call into a service owned by that process can deadlock. Shell skips constructing <code>ScreenShield</code> only when the native accessor reports all of the following:</p><ol><li><code>CapabilityVersion &gt;= 1</code>;</li><li>Mutter&#39;s <code>get_gnoblin_session_lock_launcher_ready()</code> confirms the fixed trusted Bingux lock-client path is executable;</li><li>a server with compositor-enforced <code>unlocked</code>, <code>covering</code>, <code>locked</code>, and <code>failsafe</code> states is installed; and</li><li>the coordinator already owns <code>org.gnome.ScreenSaver</code> and <code>org.gnome.Shell.ScreenShield</code> compatibility names.</li></ol><p>Shell obtains the last condition from the external broker, never from the in-process compositor endpoint. It makes bounded 1-second session-bus calls to <code>org.gnoblin.Lock</code> at <code>/org/gnoblin/Lock</code>, reads its <code>CompatibilityReady</code> property, then verifies that <code>org.gnoblin.Lock</code>, <code>org.gnome.ScreenSaver</code>, and <code>org.gnome.Shell.ScreenShield</code> all have the same D-Bus owner. The broker keeps <code>CompatibilityReady</code> false until logind and idle forwarding run and it owns both names. Any timeout, error, missing name, or owner mismatch retains GNOME ScreenShield.</p><p>The native seam also supplies <code>get_gnoblin_session_lock_active()</code> and <code>request_gnoblin_session_lock(reason)</code>. Its future coordinator exposes <code>State</code> (<code>unavailable</code>, <code>unlocked</code>, <code>covering</code>, <code>locked</code>, or <code>failsafe</code>), <code>Active</code>, <code>StateChanged</code>, and <code>RequestLock(reason)</code> on <code>org.gnoblin.SessionLock</code> for external clients. The native active accessor is true from <code>covering</code> through <code>failsafe</code>, so Shell&#39;s bridge stops work as soon as Mutter installs its input embargo rather than waiting for presentation confirmation. <code>RequestLock</code> only confirms that acquisition began; it does not substitute for the compositor presentation confirmation. The coordinator owns logind and the two compatibility D-Bus names during cutover, while GNOME Shell owns all of them during fallback. This single-owner rule prevents competing ScreenSaver methods and sleep listeners.</p><p>Gnoblin&#39;s bridge and developer console use the same adapter for their locked state. It combines stock <code>sessionMode.isLocked</code> with the coordinator&#39;s active state, cancels bridge interaction when the compositor becomes active, and refuses screenshots, previews and window operations while locked. A coordinator which is absent, too old, <code>covering</code>, or <code>unavailable</code> leaves GNOME&#39;s normal ScreenShield untouched.</p><p>The Shell screenshot service applies that same predicate before creating a <code>Shell.Screenshot</code>, opening screenshot or recording UI, interactive capture, and area selection. It returns permission denied over <code>org.gnome.Shell.Screenshot</code> from <code>covering</code> onward. This complements the compositor&#39;s capture isolation and prevents Shell-owned screenshot paths from leaking a frame during the handover.</p><p>The Shell suppresses GNOME&#39;s “Screen Lock disabled” warning only after that authority check. Switch User remains unavailable during the first cutover revision because its old path locks <code>ScreenShield</code> and the native request acknowledges covering before presentation. Enable it only when the coordinator can make a presentation-confirmed lock-before-switch guarantee.</p><h2 id="release-gates" tabindex="-1">Release gates <a class="header-anchor" href="#release-gates" aria-label="Permalink to &quot;Release gates&quot;">​</a></h2><p>The replacement is ready to become the default only when a fresh installed Gnoblin session proves all of these paths:</p><ol><li>Manual lock via shortcut, <code>loginctl</code>, desktop controls and D-Bus; repeated requests do not create competing lock owners.</li><li>Idle lock at configured timeout, idle inhibition, and lock before suspend, hibernate and lid-close without an unlocked frame on resume.</li><li>No app receives keyboard, pointer, touch, shortcut, clipboard or remote input while locked; capture and portal paths cannot reveal normal content.</li><li>Lock UI crash, forced kill, hang before its first buffer, output hotplug, scale/rotation changes and GPU reset stay opaque and locked.</li><li>Authentication success, failure, cancellation and the configured PAM/GDM conversation, including more than one prompt, behave correctly.</li><li>Real multi-monitor hardware and a fresh session confirm the <code>locked</code> event follows presentation, and ordinary input/focus returns only on unlock.</li></ol><p>The previous GNOME lock remains the default while any gate is open. A nested headless test can check protocol ordering but cannot prove display scanout, hotplug, VT or resume behaviour.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><a href="https://wayland.app/protocols/ext-session-lock-v1" target="_blank" rel="noreferrer">Wayland session-lock protocol and lifecycle</a></li><li><a href="https://github.com/hyprwm/Hyprland/blob/main/src/managers/SessionLockManager.cpp" target="_blank" rel="noreferrer">Hyprland session lock manager</a></li><li><a href="https://github.com/hyprwm/hyprlock" target="_blank" rel="noreferrer">hyprlock client</a> and <a href="https://github.com/hyprwm/hypridle" target="_blank" rel="noreferrer">hypridle policy</a></li><li><a href="https://www.freedesktop.org/software/systemd/man/latest/org.freedesktop.login1.Session.html" target="_blank" rel="noreferrer">systemd logind session API</a></li><li><a href="https://github.com/systemd/systemd/blob/main/docs/INHIBITOR_LOCKS.md" target="_blank" rel="noreferrer">systemd inhibitor locks</a></li><li><a href="https://gitlab.gnome.org/GNOME/gnome-shell/-/blob/main/js/ui/screenShield.js" target="_blank" rel="noreferrer">GNOME Shell <code>ScreenShield</code></a></li></ul>`,31)])])}const g=o(a,[["render",r]]);export{u as __pageData,g as default};
