diff --git a/src/access.c b/src/access.c
index 05bd579..e28ea80 100644
--- a/src/access.c
+++ b/src/access.c
@@ -1,4 +1,5 @@
 #include "config.h"
+#include "gnoblin-portal-policy.h"
 
 #include <glib/gi18n.h>
 
@@ -287,6 +288,22 @@ handle_access_dialog (XdpImplAccess         *object,
                       const char            *arg_body,
                       GVariant              *arg_options)
 {
+  g_auto(GnoblinPermission) permission = gnoblin_permission_for_request (
+    invocation, "access", arg_app_id, arg_handle);
+  if (permission.level == GNOBLIN_PERMISSION_DENY || permission.level == GNOBLIN_PERMISSION_ALLOW)
+    {
+      GVariantBuilder results;
+      g_variant_builder_init (&results, G_VARIANT_TYPE_VARDICT);
+      /* Choices are separate user input. Preserve the dialog when they exist. */
+      g_autoptr(GVariant) choices = g_variant_lookup_value (arg_options, "choices", NULL);
+      if (permission.level == GNOBLIN_PERMISSION_DENY || !choices || !g_variant_n_children (choices))
+        {
+          xdp_impl_access_complete_access_dialog (object, invocation,
+            permission.level == GNOBLIN_PERMISSION_ALLOW ? 0 : 1, g_variant_builder_end (&results));
+          return TRUE;
+        }
+      g_variant_builder_clear (&results);
+    }
   GxdpExternalWindow *external_parent = NULL;
 
  if (arg_parent_window)
diff --git a/src/gnoblin-portal-grants.c b/src/gnoblin-portal-grants.c
index 1b17815..f2e53db 100644
--- a/src/gnoblin-portal-grants.c
+++ b/src/gnoblin-portal-grants.c
@@ -11,8 +11,6 @@
 
 #include "gnoblin-portal-grants.h"
 
-#define PORTAL_DESKTOP_NAME "org.freedesktop.portal.Desktop"
-#define REQUEST_PATH_PREFIX "/org/freedesktop/portal/desktop/request/"
 #define GRANT_GROUP "Grant"
 #define GRANT_VERSION 1
 
@@ -30,204 +28,6 @@ grant_kind_name (GnoblinPortalGrantKind kind)
   g_assert_not_reached ();
 }
 
-static GVariant *
-call_bus_method (GDBusConnection    *connection,
-                 const char         *method,
-                 GVariant           *parameters,
-                 const GVariantType *reply_type,
-                 GError            **error)
-{
-  return g_dbus_connection_call_sync (connection,
-                                      "org.freedesktop.DBus",
-                                      "/org/freedesktop/DBus",
-                                      "org.freedesktop.DBus",
-                                      method,
-                                      parameters,
-                                      reply_type,
-                                      G_DBUS_CALL_FLAGS_NONE,
-                                      2000,
-                                      NULL,
-                                      error);
-}
-
-static char *
-portal_frontend_owner (GDBusConnection *connection)
-{
-  g_autoptr(GError) error = NULL;
-  g_autoptr(GVariant) reply = NULL;
-  const char *owner;
-
-  reply = call_bus_method (connection,
-                           "GetNameOwner",
-                           g_variant_new ("(s)", PORTAL_DESKTOP_NAME),
-                           G_VARIANT_TYPE ("(s)"),
-                           &error);
-  if (!reply)
-    {
-      g_debug ("gnoblin: cannot resolve the desktop portal owner: %s",
-               error->message);
-      return NULL;
-    }
-
-  g_variant_get (reply, "(&s)", &owner);
-  return g_strdup (owner);
-}
-
-static char *
-request_sender_from_handle (const char *request_handle)
-{
-  const char *component;
-  const char *component_end;
-  g_autofree char *decoded = NULL;
-  char *separator;
-
-  if (!request_handle || !g_str_has_prefix (request_handle, REQUEST_PATH_PREFIX))
-    return NULL;
-
-  component = request_handle + strlen (REQUEST_PATH_PREFIX);
-  component_end = strchr (component, '/');
-  if (!component_end || component_end == component || component_end[1] == '\0')
-    return NULL;
-
-  decoded = g_strndup (component, component_end - component);
-  separator = strchr (decoded, '_');
-  if (!separator || strchr (separator + 1, '_'))
-    return NULL;
-
-  for (const char *p = decoded; *p; p++)
-    {
-      if (p == separator)
-        continue;
-      if (!g_ascii_isdigit (*p))
-        return NULL;
-    }
-
-  if (separator == decoded || separator[1] == '\0')
-    return NULL;
-
-  *separator = '.';
-  return g_strconcat (":", decoded, NULL);
-}
-
-static gboolean
-connection_matches_user (GDBusConnection *connection,
-                         const char      *sender)
-{
-  g_autoptr(GError) error = NULL;
-  g_autoptr(GVariant) reply = NULL;
-  guint32 uid;
-
-  reply = call_bus_method (connection,
-                           "GetConnectionUnixUser",
-                           g_variant_new ("(s)", sender),
-                           G_VARIANT_TYPE ("(u)"),
-                           &error);
-  if (!reply)
-    {
-      g_debug ("gnoblin: cannot resolve portal requester %s: %s",
-               sender,
-               error->message);
-      return FALSE;
-    }
-
-  g_variant_get (reply, "(u)", &uid);
-  return uid == getuid ();
-}
-
-static char *
-host_executable_identity (GDBusConnection *connection,
-                          const char      *sender)
-{
-  g_autoptr(GError) error = NULL;
-  g_autoptr(GVariant) reply = NULL;
-  g_autofree char *proc_link = NULL;
-  g_autofree char *exe_path = NULL;
-  g_autofree char *canonical_path = NULL;
-  guint32 pid;
-
-  reply = call_bus_method (connection,
-                           "GetConnectionUnixProcessID",
-                           g_variant_new ("(s)", sender),
-                           G_VARIANT_TYPE ("(u)"),
-                           &error);
-  if (!reply)
-    {
-      g_debug ("gnoblin: cannot resolve host portal requester %s: %s",
-               sender,
-               error->message);
-      return NULL;
-    }
-
-  g_variant_get (reply, "(u)", &pid);
-  if (pid == 0)
-    return NULL;
-
-  proc_link = g_strdup_printf ("/proc/%u/exe", pid);
-  exe_path = g_file_read_link (proc_link, &error);
-  if (!exe_path)
-    {
-      g_debug ("gnoblin: cannot read host portal requester executable: %s",
-               error->message);
-      return NULL;
-    }
-
-  if (!g_path_is_absolute (exe_path))
-    return NULL;
-
-  canonical_path = g_canonicalize_filename (exe_path, NULL);
-  if (!g_file_test (canonical_path, G_FILE_TEST_IS_REGULAR))
-    return NULL;
-
-  return g_strconcat ("host-exe:", canonical_path, NULL);
-}
-
-char *
-gnoblin_portal_requester_identity (GDBusConnection *connection,
-                                    const char      *backend_sender,
-                                    const char      *app_id,
-                                    const char      *request_handle)
-{
-  g_autofree char *frontend_owner = NULL;
-  g_autofree char *request_sender = NULL;
-
-  g_return_val_if_fail (G_IS_DBUS_CONNECTION (connection), NULL);
-
-  frontend_owner = portal_frontend_owner (connection);
-  if (!frontend_owner || g_strcmp0 (frontend_owner, backend_sender) != 0)
-    {
-      g_debug ("gnoblin: refusing persistent grant from a direct backend caller");
-      return NULL;
-    }
-
-  request_sender = request_sender_from_handle (request_handle);
-  if (!request_sender || !connection_matches_user (connection, request_sender))
-    {
-      g_debug ("gnoblin: refusing persistent grant with an invalid or stale request handle");
-      return NULL;
-    }
-
-  if (app_id && *app_id)
-    {
-      if (!g_utf8_validate (app_id, -1, NULL))
-        return NULL;
-      return g_strconcat ("app-id:", app_id, NULL);
-    }
-
-  return host_executable_identity (connection, request_sender);
-}
-
-const char *
-gnoblin_portal_identity_name (const char *identity)
-{
-  const char *separator;
-
-  if (!identity)
-    return NULL;
-
-  separator = strchr (identity, ':');
-  return separator ? separator + 1 : NULL;
-}
-
 static char *
 grant_digest (const char *identity)
 {
diff --git a/src/gnoblin-portal-grants.h b/src/gnoblin-portal-grants.h
index e625047..b44de85 100644
--- a/src/gnoblin-portal-grants.h
+++ b/src/gnoblin-portal-grants.h
@@ -5,6 +5,7 @@
 #pragma once
 
 #include <gio/gio.h>
+#include "gnoblin-portal-identity.h"
 #include <stdint.h>
 
 #define GNOBLIN_PORTAL_GRANT_STREAMS_TYPE "a(uuv)"
@@ -26,21 +27,6 @@ typedef struct
 void gnoblin_portal_grant_clear (GnoblinPortalGrant *grant);
 G_DEFINE_AUTO_CLEANUP_CLEAR_FUNC (GnoblinPortalGrant, gnoblin_portal_grant_clear)
 
-/*
- * Verify that backend_sender owns org.freedesktop.portal.Desktop, recover the
- * original caller from request_handle, and return a namespaced identity. The
- * result is "app-id:<id>" when the trusted portal supplied an app id, or
- * "host-exe:<canonical-path>" for a live unsandboxed caller. Any failed check
- * returns NULL so callers retain one-shot consent and never persist a grant.
- */
-char *gnoblin_portal_requester_identity (GDBusConnection *connection,
-                                         const char      *backend_sender,
-                                         const char      *app_id,
-                                         const char      *request_handle);
-
-/* Return the readable part of a verified namespaced identity. */
-const char *gnoblin_portal_identity_name (const char *identity);
-
 /*
  * Load or store one portal-scoped grant. A record is keyed by a digest of its
  * namespaced identity, and the stored identity and portal kind are validated
diff --git a/src/inputcapture.c b/src/inputcapture.c
index 5ff931b..c659fb0 100644
--- a/src/inputcapture.c
+++ b/src/inputcapture.c
@@ -17,6 +17,7 @@
  */
 
 #include "config.h"
+#include "gnoblin-portal-policy.h"
 
 #include <gxdp.h>
 
@@ -444,7 +445,7 @@ handle_create_session (XdpImplInputCapture   *object,
 {
   const char *sender;
   g_autoptr(Request) request = NULL;
-  unsigned int capabilities;
+  unsigned int capabilities = 0;
 
   g_variant_lookup (arg_options, "capabilities", "u", &capabilities);
 
@@ -453,6 +454,20 @@ handle_create_session (XdpImplInputCapture   *object,
   request_export (request,
                   g_dbus_method_invocation_get_connection (invocation));
 
+  g_auto(GnoblinPermission) permission = gnoblin_permission_for_request (
+    invocation, "input-capture", arg_app_id, arg_handle);
+  if (permission.level == GNOBLIN_PERMISSION_DENY || permission.level == GNOBLIN_PERMISSION_ALLOW)
+    {
+      InputCaptureDialogHandle *handle = g_new0 (InputCaptureDialogHandle, 1);
+      handle->request = g_object_ref (request);
+      handle->session_handle = g_strdup (arg_session_handle);
+      handle->create_session_invocation = invocation;
+      handle->capabilities = capabilities;
+      on_input_capture_dialog_done_cb (NULL,
+        permission.level == GNOBLIN_PERMISSION_ALLOW ? GTK_RESPONSE_OK : GTK_RESPONSE_CANCEL, handle);
+      return TRUE;
+    }
+
   create_input_capture_dialog (invocation, request,
                                arg_parent_window,
                                arg_session_handle,
diff --git a/src/meson.build b/src/meson.build
index b1a32fd..c9c08a8 100644
--- a/src/meson.build
+++ b/src/meson.build
@@ -92,6 +92,7 @@ deps = [
 ]
 
 sources = built_sources + files(
+  'gnoblin-portal-policy.c',
   'access.c',
   'account.c',
   'accountdialog.c',
@@ -107,7 +108,7 @@ sources = built_sources + files(
   'inputcapture.c',
   'inputcapturedialog.c',
   'globalshortcuts.c',
-  'gnoblin-portal-grants.c',
+  'gnoblin-portal-identity.c',
   'gnomeinputcapture.c',
   'gnomescreencast.c',
   'lockdown.c',
@@ -150,6 +151,7 @@ gnoblin_portal_grants_test = executable(
   'test-gnoblin-portal-grants',
   files(
     'gnoblin-portal-grants.c',
+    'gnoblin-portal-identity.c',
     'test-gnoblin-portal-grants.c',
   ),
   dependencies: [
diff --git a/src/remotedesktop.c b/src/remotedesktop.c
index 22ae17b..134da6d 100644
--- a/src/remotedesktop.c
+++ b/src/remotedesktop.c
@@ -31,7 +31,8 @@
 #include "remotedesktopdialog.h"
 #include "screencastdialog.h"
 #include "gnomescreencast.h"
-#include "gnoblin-portal-grants.h"
+#include "gnoblin-portal-identity.h"
+#include "gnoblin-portal-policy.h"
 #include "request.h"
 #include "session.h"
 #include "utils.h"
@@ -95,14 +96,8 @@ typedef struct _RemoteDesktopSession
   GDBusMethodInvocation *start_invocation;
   RemoteDesktopDialogHandle *dialog_handle;
 
-  /* Verified identity and the exact capabilities pending successful start. */
-  char *gnoblin_grant_identity;
-  struct {
-    gboolean active;
-    RemoteDesktopDeviceType device_types;
-    gboolean clipboard_enabled;
-    GVariant *streams;
-  } gnoblin_pending_grant;
+  /* Verified identity used only for central permission decisions. */
+  char *gnoblin_identity;
 } RemoteDesktopSession;
 
 typedef struct _RemoteDesktopSessionClass
@@ -234,37 +229,6 @@ handle_close (XdpImplRequest *object,
   return FALSE;
 }
 
-static void
-clear_pending_grant (RemoteDesktopSession *remote_desktop_session)
-{
-  remote_desktop_session->gnoblin_pending_grant.active = FALSE;
-  remote_desktop_session->gnoblin_pending_grant.device_types = 0;
-  remote_desktop_session->gnoblin_pending_grant.clipboard_enabled = FALSE;
-  g_clear_pointer (&remote_desktop_session->gnoblin_pending_grant.streams,
-                   g_variant_unref);
-}
-
-static void
-store_pending_grant (RemoteDesktopSession *remote_desktop_session)
-{
-  g_autoptr(GError) error = NULL;
-
-  if (!remote_desktop_session->gnoblin_grant_identity ||
-      !remote_desktop_session->gnoblin_pending_grant.active)
-    return;
-
-  if (!gnoblin_portal_grant_store (
-        GNOBLIN_PORTAL_GRANT_REMOTE_DESKTOP,
-        remote_desktop_session->gnoblin_grant_identity,
-        remote_desktop_session->gnoblin_pending_grant.device_types,
-        remote_desktop_session->gnoblin_pending_grant.clipboard_enabled,
-        remote_desktop_session->gnoblin_pending_grant.streams,
-        &error))
-    g_warning ("gnoblin: failed to store remote desktop grant: %s",
-               error->message);
-
-  clear_pending_grant (remote_desktop_session);
-}
 
 static void
 remote_desktop_dialog_done (GtkWidget                 *widget,
@@ -300,26 +264,6 @@ remote_desktop_dialog_done (GtkWidget                 *widget,
     {
       RemoteDesktopSession *session = dialog_handle->session;
       g_autoptr(GError) error = NULL;
-      gboolean remember_permissions;
-
-      remember_permissions =
-        remote_desktop_dialog_get_always_allow (
-          REMOTE_DESKTOP_DIALOG (widget));
-
-      clear_pending_grant (session);
-      if (remember_permissions &&
-          session->gnoblin_grant_identity &&
-          (!session->select.screen_cast_enable ||
-           screen_cast_selection_can_persist (&session->select.screen_cast)))
-        {
-          session->gnoblin_pending_grant.active = TRUE;
-          session->gnoblin_pending_grant.device_types = device_types;
-          session->gnoblin_pending_grant.clipboard_enabled = clipboard_enabled;
-          if (streams)
-            session->gnoblin_pending_grant.streams =
-              serialize_screen_cast_streams (streams);
-        }
-
       if (!start_session (session,
                           device_types,
                           streams,
@@ -327,7 +271,7 @@ remote_desktop_dialog_done (GtkWidget                 *widget,
                           persist_allowed,
                           &error))
         {
-          clear_pending_grant (session);
+
           g_warning ("Failed to start session: %s", error->message);
           response = 2;
         }
@@ -349,8 +293,6 @@ create_remote_desktop_dialog (RemoteDesktopSession *session,
   GdkSurface *surface;
   GtkWidget *fake_parent;
   GtkWindow *dialog;
-  const char *grant_identity_name = NULL;
-
 
   if (parent_window)
     {
@@ -364,12 +306,6 @@ create_remote_desktop_dialog (RemoteDesktopSession *session,
       external_parent = NULL;
     }
 
-  if (session->gnoblin_grant_identity &&
-      (!session->select.screen_cast_enable ||
-       screen_cast_selection_can_persist (&session->select.screen_cast)))
-    grant_identity_name =
-      gnoblin_portal_identity_name (session->gnoblin_grant_identity);
-
   fake_parent = g_object_new (GTK_TYPE_WINDOW, NULL);
   g_object_ref_sink (fake_parent);
 
@@ -379,8 +315,7 @@ create_remote_desktop_dialog (RemoteDesktopSession *session,
                                            session->select.screen_cast_enable ?
                                              &session->select.screen_cast : NULL,
                                            session->clipboard.clipboard_requested,
-                                           session->persist_mode,
-                                           grant_identity_name));
+                                           session->persist_mode));
   gtk_window_set_transient_for (dialog, GTK_WINDOW (fake_parent));
   gtk_window_set_modal (dialog, TRUE);
 
@@ -692,8 +627,6 @@ start_done (RemoteDesktopSession *remote_desktop_session)
             {
               g_warning ("Failed to enable clipboard: %s", error->message);
               remote_desktop_session->clipboard.clipboard_enabled = FALSE;
-              remote_desktop_session->gnoblin_pending_grant.clipboard_enabled =
-                FALSE;
               disconnect_clipboard_handlers (remote_desktop_session);
             }
         }
@@ -737,8 +670,6 @@ start_done (RemoteDesktopSession *remote_desktop_session)
         }
     }
 
-  store_pending_grant (remote_desktop_session);
-
   xdp_impl_remote_desktop_complete_start (XDP_IMPL_REMOTE_DESKTOP (impl),
                                           remote_desktop_session->start_invocation,
                                           0,
@@ -835,7 +766,6 @@ cancel_start_session (RemoteDesktopSession *remote_desktop_session,
 {
   GVariantBuilder results_builder;
 
-  clear_pending_grant (remote_desktop_session);
   g_variant_builder_init (&results_builder, G_VARIANT_TYPE_VARDICT);
   xdp_impl_remote_desktop_complete_start (XDP_IMPL_REMOTE_DESKTOP (impl),
                                           remote_desktop_session->start_invocation,
@@ -890,84 +820,6 @@ restore_from_data (RemoteDesktopSession *remote_desktop_session)
   return TRUE;
 }
 
-typedef enum
-{
-  GRANT_START_NOT_APPLIED,
-  GRANT_START_SUCCEEDED,
-  GRANT_START_FAILED,
-} GrantStartResult;
-
-static GrantStartResult
-start_from_persistent_grant (RemoteDesktopSession *remote_desktop_session)
-{
-  g_auto(GnoblinPortalGrant) grant = { 0 };
-  g_autoptr(GVariantIter) streams_iter = NULL;
-  g_autoptr(GPtrArray) streams = NULL;
-  g_autoptr(GError) error = NULL;
-  RemoteDesktopDeviceType requested_devices;
-  gboolean requested_clipboard;
-
-  if (!remote_desktop_session->gnoblin_grant_identity)
-    return GRANT_START_NOT_APPLIED;
-
-  if (!gnoblin_portal_grant_load (
-        GNOBLIN_PORTAL_GRANT_REMOTE_DESKTOP,
-        remote_desktop_session->gnoblin_grant_identity,
-        &grant,
-        &error))
-    {
-      if (error &&
-          !g_error_matches (error, G_FILE_ERROR, G_FILE_ERROR_NOENT))
-        g_warning ("gnoblin: ignoring invalid remote desktop grant: %s",
-                   error->message);
-      return GRANT_START_NOT_APPLIED;
-    }
-
-  requested_devices = remote_desktop_session->select.device_types;
-  requested_clipboard =
-    remote_desktop_session->clipboard.clipboard_requested;
-
-  if ((grant.device_types & ~REMOTE_DESKTOP_DEVICE_TYPE_ALL) != 0 ||
-      (requested_devices & ~grant.device_types) != 0 ||
-      (requested_clipboard && !grant.clipboard_enabled))
-    {
-      g_debug ("gnoblin: remote desktop request exceeds stored capabilities");
-      return GRANT_START_NOT_APPLIED;
-    }
-
-  if (remote_desktop_session->select.screen_cast_enable)
-    {
-      if (!grant.streams)
-        return GRANT_START_NOT_APPLIED;
-
-      streams_iter = g_variant_iter_new (grant.streams);
-      streams = restore_screen_cast_streams (
-        streams_iter,
-        &remote_desktop_session->select.screen_cast);
-      if (!streams)
-        {
-          g_debug ("gnoblin: stored remote desktop monitor selection is unavailable");
-          return GRANT_START_NOT_APPLIED;
-        }
-    }
-
-  if (!start_session (remote_desktop_session,
-                      requested_devices,
-                      streams,
-                      requested_clipboard,
-                      TRUE,
-                      &error))
-    {
-      g_warning ("gnoblin: remote desktop grant start failed: %s",
-                 error->message);
-      return GRANT_START_FAILED;
-    }
-
-  g_debug ("gnoblin: restored approved remote desktop capabilities for '%s'",
-           remote_desktop_session->gnoblin_grant_identity);
-  return GRANT_START_SUCCEEDED;
-}
-
 static gboolean
 handle_start (XdpImplRemoteDesktop *object,
               GDBusMethodInvocation *invocation,
@@ -1003,29 +855,57 @@ handle_start (XdpImplRemoteDesktop *object,
 
   remote_desktop_session->start_invocation = invocation;
 
-  g_clear_pointer (&remote_desktop_session->gnoblin_grant_identity, g_free);
-  remote_desktop_session->gnoblin_grant_identity =
+  g_clear_pointer (&remote_desktop_session->gnoblin_identity, g_free);
+  remote_desktop_session->gnoblin_identity =
     gnoblin_portal_requester_identity (
       g_dbus_method_invocation_get_connection (invocation),
       sender,
       arg_app_id,
       arg_handle);
 
-  if (!restore_from_data (remote_desktop_session))
+  g_auto(GnoblinPermission) permission = gnoblin_permission_check (
+    invocation, "remote-desktop", remote_desktop_session->gnoblin_identity);
+  g_auto(GnoblinPermission) screen_permission = { 0 };
+  if (remote_desktop_session->select.screen_cast_enable)
     {
-      RemoteDesktopDialogHandle *dialog_handle;
-      GrantStartResult grant_result;
-
-      grant_result = start_from_persistent_grant (remote_desktop_session);
-      if (grant_result != GRANT_START_NOT_APPLIED)
+      screen_permission = gnoblin_permission_check (
+        invocation, "screen-cast", remote_desktop_session->gnoblin_identity);
+      if (screen_permission.level == GNOBLIN_PERMISSION_DENY)
+        permission.level = GNOBLIN_PERMISSION_DENY;
+      else if (screen_permission.level == GNOBLIN_PERMISSION_ASK &&
+               permission.level != GNOBLIN_PERMISSION_DENY)
+        permission.level = GNOBLIN_PERMISSION_ASK;
+    }
+  if (permission.level == GNOBLIN_PERMISSION_DENY)
+    {
+      cancel_start_session (remote_desktop_session, 2);
+      if (request->exported)
+        request_unexport (request);
+      return TRUE;
+    }
+  if (permission.level == GNOBLIN_PERMISSION_ALLOW)
+    {
+      g_autoptr(GError) error = NULL;
+      g_autoptr(GPtrArray) streams = NULL;
+      guint32 devices = remote_desktop_session->select.device_types;
+      gboolean clipboard = remote_desktop_session->clipboard.clipboard_requested;
+      gboolean valid = !(devices & ~permission.devices) && (!clipboard || permission.clipboard);
+      if (remote_desktop_session->select.screen_cast_enable)
         {
-          if (grant_result == GRANT_START_FAILED)
-            cancel_start_session (remote_desktop_session, 2);
-          if (request->exported)
-            request_unexport (request);
-          return TRUE;
+          streams = gnoblin_policy_monitor_streams (
+            screen_permission.level == GNOBLIN_PERMISSION_ALLOW ? screen_permission.monitors : permission.monitors,
+            &remote_desktop_session->select.screen_cast);
+          valid = valid && streams != NULL;
         }
-
+      if (!valid || !start_session (remote_desktop_session, devices, streams, clipboard, TRUE, &error))
+        cancel_start_session (remote_desktop_session, 2);
+      if (request->exported)
+        request_unexport (request);
+      return TRUE;
+    }
+  if (permission.level != GNOBLIN_PERMISSION_DEFAULT || !restore_from_data (remote_desktop_session))
+    {
+      RemoteDesktopDialogHandle *dialog_handle;
       dialog_handle = create_remote_desktop_dialog (remote_desktop_session,
                                                     invocation,
                                                     request,
@@ -1036,7 +916,7 @@ handle_start (XdpImplRemoteDesktop *object,
   return TRUE;
 
 err:
-  g_variant_builder_init (&results_builder, G_VARIANT_TYPE ("a(ua{sv}"));
+  g_variant_builder_init (&results_builder, G_VARIANT_TYPE_VARDICT);
   xdp_impl_remote_desktop_complete_start (object, invocation, 2,
                                           g_variant_builder_end (&results_builder));
 
@@ -1482,7 +1362,6 @@ remote_desktop_session_close (Session *session)
   OrgGnomeMutterRemoteDesktopSession *session_proxy;
   GnomeScreenCastSession *gnome_screen_cast_session;
   g_autoptr(GError) error = NULL;
-  clear_pending_grant (remote_desktop_session);
 
 
   gnome_screen_cast_session = remote_desktop_session->gnome_screen_cast_session;
@@ -1516,8 +1395,8 @@ remote_desktop_session_finalize (GObject *object)
   g_clear_pointer (&remote_desktop_session->streams_to_restore,
                    g_ptr_array_unref);
   g_clear_pointer (&remote_desktop_session->restored.data, g_variant_unref);
-  g_clear_pointer (&remote_desktop_session->gnoblin_grant_identity, g_free);
-  clear_pending_grant (remote_desktop_session);
+  g_clear_pointer (&remote_desktop_session->gnoblin_identity, g_free);
+
   g_free (remote_desktop_session->mutter_session_path);
 
   G_OBJECT_CLASS (remote_desktop_session_parent_class)->finalize (object);
diff --git a/src/remotedesktopdialog.c b/src/remotedesktopdialog.c
index 344620c..0466ae2 100644
--- a/src/remotedesktopdialog.c
+++ b/src/remotedesktopdialog.c
@@ -33,7 +33,6 @@ struct _RemoteDesktopDialog
   AdwSwitchRow *allow_remote_interaction_switch;
   AdwSwitchRow *allow_remote_clipboard_switch;
   GtkCheckButton *persist_check;
-  GtkCheckButton *gnoblin_always_check;
   GtkWidget *screen_cast_widget;
   AdwHeaderBar *titlebar;
 
@@ -76,12 +75,6 @@ get_persist (RemoteDesktopDialog *dialog)
   return gtk_check_button_get_active (dialog->persist_check);
 }
 
-gboolean
-remote_desktop_dialog_get_always_allow (RemoteDesktopDialog *dialog)
-{
-  return gtk_check_button_get_active (dialog->gnoblin_always_check);
-}
-
 static void
 button_clicked (GtkWidget *button,
                 RemoteDesktopDialog *dialog)
@@ -159,12 +152,9 @@ remote_desktop_dialog_new (const char *app_id,
                            RemoteDesktopDeviceType device_types,
                            ScreenCastSelection *screen_cast_select,
                            gboolean clipboard_requested,
-                           RemoteDesktopPersistMode persist_mode,
-                           const char *grant_identity_name)
+                           RemoteDesktopPersistMode persist_mode)
 {
   RemoteDesktopDialog *dialog;
-  g_autofree char *grant_label = NULL;
-
 
   dialog = g_object_new (REMOTE_DESKTOP_TYPE_DIALOG, NULL);
   dialog->device_types = device_types;
@@ -218,13 +208,6 @@ remote_desktop_dialog_new (const char *app_id,
       break;
     }
 
-  if (grant_identity_name)
-    grant_label = g_strdup_printf (_("Remember these permissions for %s"),
-                                   grant_identity_name);
-  gtk_check_button_set_label (dialog->gnoblin_always_check, grant_label);
-  gtk_widget_set_visible (GTK_WIDGET (dialog->gnoblin_always_check),
-                          grant_identity_name != NULL);
-
   return dialog;
 }
 
@@ -272,7 +255,6 @@ remote_desktop_dialog_class_init (RemoteDesktopDialogClass *klass)
   gtk_widget_class_bind_template_child (widget_class, RemoteDesktopDialog, allow_remote_interaction_switch);
   gtk_widget_class_bind_template_child (widget_class, RemoteDesktopDialog, allow_remote_clipboard_switch);
   gtk_widget_class_bind_template_child (widget_class, RemoteDesktopDialog, persist_check);
-  gtk_widget_class_bind_template_child (widget_class, RemoteDesktopDialog, gnoblin_always_check);
   gtk_widget_class_bind_template_child (widget_class, RemoteDesktopDialog, screen_cast_widget);
   gtk_widget_class_bind_template_child (widget_class, RemoteDesktopDialog, titlebar);
   gtk_widget_class_bind_template_callback (widget_class, button_clicked);
diff --git a/src/remotedesktopdialog.h b/src/remotedesktopdialog.h
index 492f8f4..6ef88cf 100644
--- a/src/remotedesktopdialog.h
+++ b/src/remotedesktopdialog.h
@@ -31,8 +31,4 @@ RemoteDesktopDialog * remote_desktop_dialog_new (const char *app_id,
                                                  RemoteDesktopDeviceType device_types,
                                                  ScreenCastSelection *screen_cast_select,
                                                  gboolean clipboard_requested,
-                                                 RemoteDesktopPersistMode persist_mode,
-                                                 const char *grant_identity_name);
-
-/* gnoblin: TRUE if the visible per-app permission checkbox was selected. */
-gboolean remote_desktop_dialog_get_always_allow (RemoteDesktopDialog *dialog);
+                                                 RemoteDesktopPersistMode persist_mode);
diff --git a/src/remotedesktopdialog.ui b/src/remotedesktopdialog.ui
index 0ea33e0..d2051e1 100644
--- a/src/remotedesktopdialog.ui
+++ b/src/remotedesktopdialog.ui
@@ -72,15 +72,6 @@
                       </object>
                     </child>
 
-                    <!-- gnoblin: macOS-style per-app persistent grant -->
-                    <child>
-                      <object class="GtkCheckButton" id="gnoblin_always_check">
-                        <property name="visible">False</property>
-                        <property name="active">False</property>
-                        <property name="halign">center</property>
-                      </object>
-                    </child>
-
                     <child>
                       <object class="GtkListBox">
                         <property name="valign">start</property>
diff --git a/src/screencast.c b/src/screencast.c
index 12602cb..968defa 100644
--- a/src/screencast.c
+++ b/src/screencast.c
@@ -29,7 +29,8 @@
 #include "screencastwidget.h"
 #include "screencastdialog.h"
 #include "gnomescreencast.h"
-#include "gnoblin-portal-grants.h"
+#include "gnoblin-portal-identity.h"
+#include "gnoblin-portal-policy.h"
 #include "remotedesktop.h"
 #include "displaystatetracker.h"
 #include "request.h"
@@ -67,9 +68,8 @@ typedef struct _ScreenCastSession
   GDBusMethodInvocation *start_invocation;
   ScreenCastDialogHandle *dialog_handle;
 
-  /* Verified, namespaced identity for portal-scoped persistent grants. */
-  char *gnoblin_grant_identity;
-  GVariant *gnoblin_pending_grant_streams;
+  /* Verified identity used only for central permission decisions. */
+  char *gnoblin_identity;
 } ScreenCastSession;
 
 typedef struct _ScreenCastSessionClass
@@ -161,16 +161,6 @@ serialize_screen_cast_streams_to_builder (GPtrArray       *streams,
     }
 }
 
-GVariant *
-serialize_screen_cast_streams (GPtrArray *streams)
-{
-  GVariantBuilder builder;
-
-  g_variant_builder_init (&builder, G_VARIANT_TYPE (SCREEN_CAST_STREAMS_VARIANT_TYPE));
-  serialize_screen_cast_streams_to_builder (streams, &builder);
-  return g_variant_ref_sink (g_variant_builder_end (&builder));
-}
-
 void
 serialize_screen_cast_streams_as_restore_data (GPtrArray       *streams,
                                                GVariantBuilder *impl_builder)
@@ -213,42 +203,12 @@ serialize_session_as_restore_data (ScreenCastSession *screen_cast_session,
   return g_variant_builder_end (&restore_data_builder);
 }
 
-static void
-clear_pending_grant (ScreenCastSession *screen_cast_session)
-{
-  g_clear_pointer (&screen_cast_session->gnoblin_pending_grant_streams,
-                   g_variant_unref);
-}
-
-static void
-store_pending_grant (ScreenCastSession *screen_cast_session)
-{
-  g_autoptr(GError) error = NULL;
-
-  if (!screen_cast_session->gnoblin_grant_identity ||
-      !screen_cast_session->gnoblin_pending_grant_streams)
-    return;
-
-  if (!gnoblin_portal_grant_store (
-        GNOBLIN_PORTAL_GRANT_SCREEN_CAST,
-        screen_cast_session->gnoblin_grant_identity,
-        0,
-        FALSE,
-        screen_cast_session->gnoblin_pending_grant_streams,
-        &error))
-    g_warning ("gnoblin: failed to store screen cast grant: %s",
-               error->message);
-
-  clear_pending_grant (screen_cast_session);
-}
-
 static void
 cancel_start_session (ScreenCastSession *screen_cast_session,
                       int response)
 {
   GVariantBuilder results_builder;
 
-  clear_pending_grant (screen_cast_session);
   g_variant_builder_init (&results_builder, G_VARIANT_TYPE_VARDICT);
   xdp_impl_screen_cast_complete_start (XDP_IMPL_SCREEN_CAST (impl),
                                        screen_cast_session->start_invocation,
@@ -305,8 +265,6 @@ on_gnome_screen_cast_session_ready (GnomeScreenCastSession *gnome_screen_cast_se
         }
     }
 
-  store_pending_grant (screen_cast_session);
-
   xdp_impl_screen_cast_complete_start (XDP_IMPL_SCREEN_CAST (impl),
                                        screen_cast_session->start_invocation, 0,
                                        g_variant_builder_end (&results_builder));
@@ -317,7 +275,7 @@ static void
 on_gnome_screen_cast_session_closed (GnomeScreenCastSession *gnome_screen_cast_session,
                                      ScreenCastSession      *screen_cast_session)
 {
-  clear_pending_grant (screen_cast_session);
+
   session_close ((Session *)screen_cast_session, TRUE);
 }
 
@@ -392,24 +350,12 @@ on_screen_cast_dialog_done_cb (GtkWidget              *widget,
       ScreenCastSession *screen_cast_session = dialog_handle->session;
       g_autoptr(GError) error = NULL;
 
-      gboolean remember_selection;
-
-      remember_selection =
-        screen_cast_dialog_get_always_allow (SCREEN_CAST_DIALOG (widget));
-
       screen_cast_session->persist_mode = MIN (screen_cast_session->persist_mode,
                                                persist_mode);
 
-      clear_pending_grant (screen_cast_session);
-      if (remember_selection &&
-          screen_cast_session->gnoblin_grant_identity &&
-          screen_cast_selection_can_persist (&screen_cast_session->select))
-        screen_cast_session->gnoblin_pending_grant_streams =
-          serialize_screen_cast_streams (streams);
-
       if (!start_session (dialog_handle->session, streams, &error))
         {
-          clear_pending_grant (screen_cast_session);
+
           g_warning ("Failed to start session: %s", error->message);
           response = 2;
         }
@@ -434,7 +380,6 @@ create_screen_cast_dialog (ScreenCastSession     *session,
   GdkSurface *surface;
   GtkWidget *fake_parent;
   GtkWindow *dialog;
-  const char *grant_identity_name = NULL;
 
   if (parent_window)
     {
@@ -451,15 +396,9 @@ create_screen_cast_dialog (ScreenCastSession     *session,
   fake_parent = g_object_new (GTK_TYPE_WINDOW, NULL);
   g_object_ref_sink (fake_parent);
 
-  if (session->gnoblin_grant_identity &&
-      screen_cast_selection_can_persist (&session->select))
-    grant_identity_name =
-      gnoblin_portal_identity_name (session->gnoblin_grant_identity);
-
   dialog = GTK_WINDOW (screen_cast_dialog_new (request->app_id,
                                                &session->select,
-                                               session->persist_mode,
-                                               grant_identity_name));
+                                               session->persist_mode));
   gtk_window_set_transient_for (dialog, GTK_WINDOW (fake_parent));
   gtk_window_set_modal (dialog, TRUE);
 
@@ -669,14 +608,6 @@ restore_screen_cast_streams (GVariantIter        *streams_iter,
   return streams->len > 0 ? g_steal_pointer (&streams) : NULL;
 }
 
-gboolean
-screen_cast_selection_can_persist (ScreenCastSelection *selection)
-{
-  return selection->source_types.monitor &&
-         !selection->source_types.window &&
-         !selection->source_types.virtual_monitor;
-}
-
 static gboolean
 restore_stream_from_data (ScreenCastSession *screen_cast_session)
 {
@@ -713,55 +644,41 @@ restore_stream_from_data (ScreenCastSession *screen_cast_session)
   return TRUE;
 }
 
-typedef enum
-{
-  GRANT_START_NOT_APPLIED,
-  GRANT_START_SUCCEEDED,
-  GRANT_START_FAILED,
-} GrantStartResult;
-
-static GrantStartResult
-start_from_persistent_grant (ScreenCastSession *screen_cast_session)
+/* Exact connector selection; mirrored outputs produce one stream per logical monitor. */
+GPtrArray *
+gnoblin_policy_monitor_streams (char **connectors, ScreenCastSelection *selection)
 {
-  g_auto(GnoblinPortalGrant) grant = { 0 };
-  g_autoptr(GVariantIter) streams_iter = NULL;
-  g_autoptr(GPtrArray) streams = NULL;
-  g_autoptr(GError) error = NULL;
-
-  if (!screen_cast_session->gnoblin_grant_identity)
-    return GRANT_START_NOT_APPLIED;
-
-  if (!gnoblin_portal_grant_load (
-        GNOBLIN_PORTAL_GRANT_SCREEN_CAST,
-        screen_cast_session->gnoblin_grant_identity,
-        &grant,
-        &error))
-    {
-      if (error)
-        g_warning ("gnoblin: ignoring invalid screen cast grant: %s",
-                   error->message);
-      return GRANT_START_NOT_APPLIED;
-    }
-
-  streams_iter = g_variant_iter_new (grant.streams);
-  streams = restore_screen_cast_streams (streams_iter,
-                                         &screen_cast_session->select);
-  if (!streams)
+  g_autoptr(GPtrArray) streams = g_ptr_array_new_with_free_func (
+    (GDestroyNotify) screen_cast_stream_info_free);
+  g_autoptr(GHashTable) seen = g_hash_table_new (g_direct_hash, g_direct_equal);
+  GList *logical = display_state_tracker_get_logical_monitors (display_state_tracker_get ());
+  if (!selection->source_types.monitor || !connectors || !connectors[0]) return NULL;
+  for (guint i = 0; connectors[i]; i++)
     {
-      g_debug ("gnoblin: stored screen cast selection is unavailable");
-      return GRANT_START_NOT_APPLIED;
-    }
-
-  if (!start_session (screen_cast_session, streams, &error))
-    {
-      g_warning ("gnoblin: screen cast grant start failed: %s",
-                 error->message);
-      return GRANT_START_FAILED;
+      gboolean found = FALSE;
+      for (GList *l = logical; l; l = l->next)
+        {
+          for (GList *m = logical_monitor_get_monitors (l->data); m; m = m->next)
+            {
+              if (!(g_str_equal (connectors[i], "primary") && logical_monitor_is_primary (l->data)) &&
+                  !g_str_equal (connectors[i], monitor_get_connector (m->data))) continue;
+              found = TRUE;
+              if (!g_hash_table_contains (seen, l->data))
+                {
+                  ScreenCastStreamInfo *info = g_new0 (ScreenCastStreamInfo, 1);
+                  info->type = SCREEN_CAST_SOURCE_TYPE_MONITOR;
+                  info->data.monitor = monitor_dup (m->data);
+                  info->id = streams->len;
+                  g_ptr_array_add (streams, info);
+                  g_hash_table_add (seen, l->data);
+                }
+              break;
+            }
+        }
+      if (!found) return NULL;
     }
-
-  g_debug ("gnoblin: restored approved screen cast selection for '%s'",
-           screen_cast_session->gnoblin_grant_identity);
-  return GRANT_START_SUCCEEDED;
+  if (!streams->len || (!selection->multiple && streams->len > 1)) return NULL;
+  return g_steal_pointer (&streams);
 }
 
 static gboolean
@@ -799,30 +716,38 @@ handle_start (XdpImplScreenCast     *object,
 
   screen_cast_session->start_invocation = invocation;
 
-  g_clear_pointer (&screen_cast_session->gnoblin_grant_identity, g_free);
-  screen_cast_session->gnoblin_grant_identity =
+  g_clear_pointer (&screen_cast_session->gnoblin_identity, g_free);
+  screen_cast_session->gnoblin_identity =
     gnoblin_portal_requester_identity (
       g_dbus_method_invocation_get_connection (invocation),
       sender,
       arg_app_id,
       arg_handle);
 
-  if (!restore_stream_from_data (screen_cast_session))
+  g_auto(GnoblinPermission) permission = gnoblin_permission_check (
+    invocation, "screen-cast", screen_cast_session->gnoblin_identity);
+  if (permission.level == GNOBLIN_PERMISSION_DENY)
+    {
+      cancel_start_session (screen_cast_session, 2);
+      if (request->exported)
+        request_unexport (request);
+      return TRUE;
+    }
+  if (permission.level == GNOBLIN_PERMISSION_ALLOW)
+    {
+      g_autoptr(GError) error = NULL;
+      g_autoptr(GPtrArray) streams = gnoblin_policy_monitor_streams (
+        permission.monitors, &screen_cast_session->select);
+      if (!streams || !start_session (screen_cast_session, streams, &error))
+        cancel_start_session (screen_cast_session, 2);
+      if (request->exported)
+        request_unexport (request);
+      return TRUE;
+    }
+  if (permission.level != GNOBLIN_PERMISSION_DEFAULT ||
+      !restore_stream_from_data (screen_cast_session))
     {
       ScreenCastDialogHandle *dialog_handle;
-      GrantStartResult grant_result;
-
-      grant_result = start_from_persistent_grant (screen_cast_session);
-      if (grant_result != GRANT_START_NOT_APPLIED)
-        {
-          if (grant_result == GRANT_START_FAILED)
-            cancel_start_session (screen_cast_session, 2);
-          if (request->exported)
-            request_unexport (request);
-          return TRUE;
-        }
-
-
       dialog_handle = create_screen_cast_dialog (screen_cast_session,
                                                  invocation,
                                                  request,
@@ -834,7 +759,7 @@ handle_start (XdpImplScreenCast     *object,
   return TRUE;
 
 err:
-  g_variant_builder_init (&results_builder, G_VARIANT_TYPE ("a(ua{sv}"));
+  g_variant_builder_init (&results_builder, G_VARIANT_TYPE_VARDICT);
   xdp_impl_screen_cast_complete_start (object, invocation, 2,
                                        g_variant_builder_end (&results_builder));
 
@@ -1082,8 +1007,8 @@ screen_cast_session_finalize (GObject *object)
 
   g_clear_pointer (&screen_cast_session->streams_to_restore, g_ptr_array_unref);
   g_clear_pointer (&screen_cast_session->restored.data, g_variant_unref);
-  g_clear_pointer (&screen_cast_session->gnoblin_grant_identity, g_free);
-  clear_pending_grant (screen_cast_session);
+  g_clear_pointer (&screen_cast_session->gnoblin_identity, g_free);
+
   g_clear_object (&screen_cast_session->gnome_screen_cast_session);
 
   G_OBJECT_CLASS (screen_cast_session_parent_class)->finalize (object);
diff --git a/src/screencast.h b/src/screencast.h
index b6ff60e..ba1cebc 100644
--- a/src/screencast.h
+++ b/src/screencast.h
@@ -82,12 +82,7 @@ void screen_cast_stream_info_free (ScreenCastStreamInfo *info);
 void serialize_screen_cast_streams_as_restore_data (GPtrArray       *streams,
                                                     GVariantBuilder *impl_builder);
 
-GVariant * serialize_screen_cast_streams (GPtrArray *streams);
-
 GPtrArray * restore_screen_cast_streams (GVariantIter *streams_iter,
                                          ScreenCastSelection *screen_cast_selection);
 
-/* Persistent grants restore the exact approved monitor set. Window matching
- * is deliberately excluded because title-based restoration can select a
- * different current window. */
-gboolean screen_cast_selection_can_persist (ScreenCastSelection *selection);
+GPtrArray *gnoblin_policy_monitor_streams (char **connectors, ScreenCastSelection *selection);
diff --git a/src/screencastdialog.c b/src/screencastdialog.c
index a9b2574..f523406 100644
--- a/src/screencastdialog.c
+++ b/src/screencastdialog.c
@@ -146,8 +146,7 @@ screen_cast_dialog_init (ScreenCastDialog *dialog)
 ScreenCastDialog *
 screen_cast_dialog_new (const char            *app_id,
                         ScreenCastSelection   *select,
-                        ScreenCastPersistMode  persist_mode,
-                        const char            *grant_identity_name)
+                        ScreenCastPersistMode  persist_mode)
 {
   ScreenCastDialog *dialog;
   ScreenCastWidget *screen_cast_widget;
@@ -159,17 +158,6 @@ screen_cast_dialog_new (const char            *app_id,
   screen_cast_widget_set_source_types (screen_cast_widget,
                                        select->source_types);
   screen_cast_widget_set_persist_mode (screen_cast_widget, persist_mode);
-  screen_cast_widget_set_persistent_identity (screen_cast_widget,
-                                              grant_identity_name);
 
   return dialog;
 }
-
-gboolean
-screen_cast_dialog_get_always_allow (ScreenCastDialog *dialog)
-{
-  ScreenCastWidget *screen_cast_widget =
-    SCREEN_CAST_WIDGET (dialog->screen_cast_widget);
-
-  return screen_cast_widget_get_always_allow (screen_cast_widget);
-}
diff --git a/src/screencastdialog.h b/src/screencastdialog.h
index 6953c4e..f349814 100644
--- a/src/screencastdialog.h
+++ b/src/screencastdialog.h
@@ -28,8 +28,4 @@ G_DECLARE_FINAL_TYPE (ScreenCastDialog, screen_cast_dialog,
 
 ScreenCastDialog * screen_cast_dialog_new (const char            *app_id,
                                            ScreenCastSelection   *select,
-                                           ScreenCastPersistMode  persist_mode,
-                                           const char            *grant_identity_name);
-
-/* gnoblin: TRUE if the user ticked "Always allow this app". */
-gboolean screen_cast_dialog_get_always_allow (ScreenCastDialog *dialog);
+                                           ScreenCastPersistMode  persist_mode);
diff --git a/src/screencastwidget.c b/src/screencastwidget.c
index 242b155..46e20b7 100644
--- a/src/screencastwidget.c
+++ b/src/screencastwidget.c
@@ -56,8 +56,6 @@ struct _ScreenCastWidget
   GtkCheckButton        *persist_check;
   ScreenCastPersistMode  persist_mode;
 
-  GtkCheckButton        *gnoblin_always_check;
-
   DisplayStateTracker   *display_state_tracker;
   ulong                  monitors_changed_handler_id;
 
@@ -526,7 +524,6 @@ screen_cast_widget_class_init (ScreenCastWidgetClass *klass)
 
   gtk_widget_class_set_template_from_resource (widget_class, "/org/freedesktop/portal/desktop/gnome/screencastwidget.ui");
   gtk_widget_class_bind_template_child (widget_class, ScreenCastWidget, persist_check);
-  gtk_widget_class_bind_template_child (widget_class, ScreenCastWidget, gnoblin_always_check);
   gtk_widget_class_bind_template_child (widget_class, ScreenCastWidget, source_type_switcher);
   gtk_widget_class_bind_template_child (widget_class, ScreenCastWidget, source_type);
   gtk_widget_class_bind_template_child (widget_class, ScreenCastWidget, window_selection_page);
@@ -764,26 +761,3 @@ screen_cast_widget_get_persist_mode (ScreenCastWidget *screen_cast_widget)
 
   return screen_cast_widget->persist_mode;
 }
-
-/* Hidden unless the requester has a verified identity and only exact monitor
- * selections can be persisted. */
-void
-screen_cast_widget_set_persistent_identity (ScreenCastWidget *screen_cast_widget,
-                                            const char       *identity_name)
-{
-  g_autofree char *label = NULL;
-
-  if (identity_name)
-    label = g_strdup_printf (_("Remember this monitor selection for %s"),
-                             identity_name);
-
-  gtk_check_button_set_label (screen_cast_widget->gnoblin_always_check, label);
-  gtk_widget_set_visible (GTK_WIDGET (screen_cast_widget->gnoblin_always_check),
-                          identity_name != NULL);
-}
-
-gboolean
-screen_cast_widget_get_always_allow (ScreenCastWidget *screen_cast_widget)
-{
-  return gtk_check_button_get_active (screen_cast_widget->gnoblin_always_check);
-}
diff --git a/src/screencastwidget.h b/src/screencastwidget.h
index 50232c7..16f5f6d 100644
--- a/src/screencastwidget.h
+++ b/src/screencastwidget.h
@@ -44,9 +44,3 @@ void screen_cast_widget_set_persist_mode (ScreenCastWidget *screen_cast_widget,
 
 ScreenCastPersistMode
 screen_cast_widget_get_persist_mode (ScreenCastWidget *screen_cast_widget);
-
-void screen_cast_widget_set_persistent_identity (ScreenCastWidget *screen_cast_widget,
-                                                  const char       *identity_name);
-
-gboolean
-screen_cast_widget_get_always_allow (ScreenCastWidget *screen_cast_widget);
diff --git a/src/screencastwidget.ui b/src/screencastwidget.ui
index 79975b6..095485b 100644
--- a/src/screencastwidget.ui
+++ b/src/screencastwidget.ui
@@ -18,16 +18,6 @@
         <property name="use-underline">True</property>
       </object>
     </child>
-
-    <!-- gnoblin: macOS-style per-app persistent grant -->
-    <child>
-      <object class="GtkCheckButton" id="gnoblin_always_check">
-        <property name="visible">False</property>
-        <property name="active">False</property>
-        <property name="halign">center</property>
-        <property name="label" translatable="yes">Always allow this app</property>
-      </object>
-    </child>
     <child>
     </child>
     <child>
diff --git a/src/screenshot.c b/src/screenshot.c
index c711904..3783d6f 100644
--- a/src/screenshot.c
+++ b/src/screenshot.c
@@ -1,6 +1,7 @@
 #define _GNU_SOURCE 1
 
 #include "config.h"
+#include "gnoblin-portal-policy.h"
 
 #include <errno.h>
 #include <locale.h>
@@ -196,6 +197,14 @@ handle_screenshot (XdpImplScreenshot *object,
                    const char *arg_parent_window,
                    GVariant *arg_options)
 {
+  g_auto(GnoblinPermission) permission = gnoblin_permission_for_request (
+    invocation, "screenshot", arg_app_id, arg_handle);
+  if (permission.level == GNOBLIN_PERMISSION_DENY)
+    {
+      xdp_impl_screenshot_complete_screenshot (object, invocation, 2,
+        g_variant_new_array (G_VARIANT_TYPE ("{sv}"), NULL, 0));
+      return TRUE;
+    }
   g_autoptr(Request) request = NULL;
   const char *sender;
   ScreenshotDialogHandle *handle;
@@ -233,6 +242,9 @@ handle_screenshot (XdpImplScreenshot *object,
       if (!g_variant_lookup (arg_options, "permission_store_checked", "b", &permission_store_checked))
         permission_store_checked = FALSE;
 
+      if (permission.level == GNOBLIN_PERMISSION_ALLOW) permission_store_checked = TRUE;
+      if (permission.level == GNOBLIN_PERMISSION_ASK) permission_store_checked = FALSE;
+
       if (arg_parent_window)
         {
           external_parent = gxdp_external_window_new_from_handle (arg_parent_window);
