From a13730256720073c5f389161edbe4b722b29db17 Mon Sep 17 00:00:00 2001
From: kierandrewett <kieran@drewett.dev>
Date: Wed, 15 Jul 2026 13:28:18 +0100
Subject: [PATCH] session: hide native top bar only in Gnoblin mode

Use the immutable primary session mode when deciding whether the native panel
is visible, reactive, and reserving a strut. Stock
GNOME and non-Gnoblin modes retain their upstream top bar behaviour.
---
 js/ui/layout.js | 3 ++-
 js/ui/panel.js  | 4 +++-
 2 files changed, 5 insertions(+), 2 deletions(-)

diff --git a/js/ui/layout.js b/js/ui/layout.js
index 47cf50c..53880b1 100644
--- a/js/ui/layout.js
+++ b/js/ui/layout.js
@@ -265,12 +265,13 @@ export const LayoutManager = GObject.registerClass({
         });
         this.addChrome(this.screenShieldGroup);
 
+        const isGnoblin = global.session_mode === 'gnoblin';
         this.panelBox = new St.BoxLayout({
             name: 'panelBox',
             orientation: Clutter.Orientation.VERTICAL,
         });
         this.addChrome(this.panelBox, {
-            affectsStruts: true,
+            affectsStruts: !isGnoblin,
             trackFullscreen: true,
         });
         this.panelBox.connect('notify::allocation',
diff --git a/js/ui/panel.js b/js/ui/panel.js
index 7e98d91..49eba5a 100644
--- a/js/ui/panel.js
+++ b/js/ui/panel.js
@@ -458,9 +458,11 @@ export class Panel extends St.Widget {
     }
 
     _init() {
+        const isGnoblin = global.session_mode === 'gnoblin';
         super._init({
             name: 'panel',
-            reactive: true,
+            opacity: isGnoblin ? 0 : 255,
+            reactive: !isGnoblin,
         });
 
         this.set_offscreen_redirect(Clutter.OffscreenRedirect.ALWAYS);
-- 
2.55.0

